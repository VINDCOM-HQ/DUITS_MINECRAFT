package io.github.netherdeck.common.bridge.core.world.level.block;

import net.minecraft.core.BlockPos;

public interface MultifaceSpreaderSpreadPosBridge {
    BlockPos source();
    void netherdeck$setSource(BlockPos source);
}
